;PROGRAM TO ENTER A STRING

[ORG 0X7C00]

[BITS 16]

JMP BEGIN

BEGIN: 
			MOV DI,STRIP
			;MOV CX,0
	READ:	MOV AX,0
			INT 0X16
			CMP AL,0DH
			JE EXIT
			MOV AH,0X0E
			INT 0X10
			STOSB
			;INC CX
			;CMP CX,32
			;JE EXIT
			JMP READ
			
	EXIT:
			HLT
			
STRIP TIMES 100 DB 0

TIMES 510-($-$$)DB 0

DW 0XAA55