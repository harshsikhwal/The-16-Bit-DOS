%MACRO CONCAT 2-*
	PUSHA
	MOV DI,%1
	%REP %0
		MOV SI,%1
		CALL COPY
		%ROTATE 1
	%ENDREP
	POPA
%ENDMACRO

COPY:
COPY_LOOP:
		LODSB
		CMP AL,0
		JE COPY_END
		STOSB
		JMP COPY_LOOP
COPY_END:
		MOV AL,32
		STOSB 
		RET